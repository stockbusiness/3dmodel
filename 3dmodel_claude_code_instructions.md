# Claude Code 実装指示 — stockbusiness/3dmodel

対象リポジトリ：https://github.com/stockbusiness/3dmodel
仕様正本：`docs/spec/3d_art_validation_spec_v1_1.md`
作業ルール：リポジトリ直下の `CLAUDE.md`

## 0. 事前準備（ユーザーが行う）

1. リポジトリに以下を配置してコミットする（mainへ直接で可、このときだけ）。
   - `CLAUDE.md`（同梱ファイル）
   - `docs/spec/3d_art_validation_spec_v1_1.md`（仕様書Ver.1.1）
2. ローカルで `claude` を起動し、下記のステージ別プロンプトを**1つずつ**投入する。1回のセッションで複数ステージを進めさせない。
3. 各ステージの完了報告を確認し、`BLOCKER` に回答してから次のステージへ進む。
4. A3以降で必要になる入力（ユーザーが用意する）：
   - Tripo / Meshy のAPIキー（`.env` に直接記入、チャットに貼らない）
   - セット上限額と全体上限額（USD）
   - VPSの接続先（FQDN、IP制限の許可元、Basic認証情報）※A4で使用
   - 試験用画像（運営・講師の自作作品20、3D向け準備画像10）※A3.5とA5で使用

---

## 1. 初回プロンプト（リポジトリ確認・A1開始前に1回だけ）

```
このリポジトリで3Dアート品質検証システム（フェーズA）を実装します。

まず以下を行ってください。実装はまだしないでください。
1. CLAUDE.md と docs/spec/3d_art_validation_spec_v1_1.md を全文読む。
2. git status、既存ファイル、ブランチ構成を確認し、ユーザーの未コミット変更があれば報告する。
3. 仕様第15章のディレクトリ構成と第16章の実装順に基づき、A1〜A5の各ステージで作成するファイル一覧と、各ステージの完了条件を docs/plan.md に日本語で書く。
4. 仕様を読んで「実装前にユーザーの判断が必要な点」があれば BLOCKER として docs/decisions.md に列挙する。判断が不要な軽微な選択は ASSUMPTION として記録し、自分で決める。
5. docs/plan.md と docs/decisions.md をコミットし、内容を報告する。

報告は日本語で、BLOCKER があれば先頭にまとめてください。
```

---

## 2. ステージ別プロンプト

### A1：骨組み・モック（ブランチ feature/a1-skeleton）

```
ブランチ feature/a1-skeleton を作成し、仕様第16章のA1を実装してください。

範囲：
- 仕様第3章の構成でプロジェクトを初期化（pyproject.toml、依存ロック、Dockerfile、compose.yaml、.env.example、README.md、VERSION、CHANGELOG.md、THIRD_PARTY_NOTICES.md）
- 第9章のデータ設計に基づくSQLAlchemyモデルとAlembic初期マイグレーション（全テーブル。使わない列もこの時点で作る）
- 第6.1章のログイン（CLIで初期管理者作成、パスワードハッシュ、HttpOnly Cookie、CSRF）
- 第6.2〜6.3章の検証セット作成・画像アップロード（利用同意を含む）・加工画像追加（加工種別は選択式）
- 第8章のMockアダプター（成功のみで可。障害切替はA2）と、疑似生成→サンプルGLB保存→ready_for_review までの最短経路
- 第6.5章の最小の表示（model-viewerをバージョン固定でstaticに同梱、元画像＋モデルの表示。ブラインドはA4）
- 第10章の評価入力（5軸＋重大不具合タグ＋判定）とCSV出力（UTF-8 BOM、数式エスケープ）
- 第12章のうち、画像のデコード検査・EXIF除去・拡張子偽装拒否・認証付きファイル配信・パストラバーサル禁止
- pytest：ログイン、アップロード、モック生成、評価、CSVの正常系。第13章の試験10・11

この段階では worker は同期的な疑似処理で構いませんが、app/worker.py の入口は作ってください。
外部API、上限額判定、送信枠、冪等キー、ブラインドはA2/A4で実装します。先回りしないでください。

完了条件：docker compose up でログイン→画像→疑似生成→GLB表示→評価→CSVまでモックで通る。README の手順で再現できる。
完了したら VERSION を 0.1.0、CHANGELOG.md を更新し、仕様第17章の形式で日本語で報告してください。
```

### A2：永続ワーカー・復旧・冪等・上限額（ブランチ feature/a2-worker）

```
ブランチ feature/a2-worker を作成し、仕様第16章のA2を実装してください。

範囲：
- 第8章：永続ジョブ処理。queued→submitting→running→downloading→ready_for_review と、状態遷移表のすべての分岐・復帰経路
- 第8章「ワーカーの処理単位」：状態確認とダウンロードを別処理単位、全外部通信にタイムアウト、DBロックを持ったまま通信しない
- 第8章のlease：BEGIN IMMEDIATE＋条件付きUPDATEでclaim、lease_owner/lease_until、期限切れ対象を新ワーカーが取得したときsubmittingのままならsubmission_unknownへ
- 第7章：冪等キー（サーバー発行フォームトークン方式、同キー・別本文は409）
- 第7章・第11章：上限額判定（セット・全体）、送信枠（asset×providerで2回）、依頼登録を同一短期トランザクションで。比較は2社分合算
- 第9章の送信枠の消費・返却ルール（provider_failedは返却、submission_unknownは保持、通信再確認・再ダウンロードは新しい試行に数えない）
- 第7章のAPI：retry、refresh、retry-download、cancel、resolve-submission、resolve-artifact
- 第13章のモック障害切替：成功、事業者失敗、429、作成応答タイムアウト、長時間待機、ダウンロード失敗、不正GLB、上限額超過
- 第13章の必須試験 1〜9、13、15、16、17、19、20

完了条件：docker compose で web と worker が別プロセスで動き、生成中に worker を再起動しても同じタスクを再確認して完了する。上記試験がすべて通る。
完了したら VERSION を 0.2.0、CHANGELOG.md を更新し、第17章の形式で日本語で報告してください。
```

### A3：Tripo/Meshyアダプター（ブランチ feature/a3-providers）

```
ブランチ feature/a3-providers を作成し、仕様第16章のA3を実装してください。

最初に、仕様第4章の公式資料を実際に読み、以下を docs/provider-contracts.md に記録してください：
- 採用するSDK/ライブラリのタグまたはコミット
- 画像→3DのモデルID、パラメーター、返却形式、エラー、タイムアウト、課金条件
- 送信画像・生成物の保持期間、学習利用の可否とopt-out手段、生成物の利用条件
- 確認日

公式資料で確認できない項目は UNVERIFIED として記録し、推測で埋めないでください。UNVERIFIED を含むプリセットは画面で「未確認」表示にし、実生成を選択不可にしてください。

範囲：
- app/providers/tripo.py（公式SDKを固定バージョンで使用。SDKの作成系自動リトライを確認し、submit では無効化または制御する）
- app/providers/meshy.py（公式契約に基づくHTTPクライアント実装）
- 第8章の共通アダプター5メソッド。cancel は公式APIが対応している場合のみ実装、未対応は unsupported を返す
- 第12章：事業者結果のダウンロードの許可ホスト・DNS/IP検査・リダイレクト再検査。SDKが独自にダウンロードする場合はその経路も同等の制限を満たすか確認し、満たせなければ自前のダウンロードに置き換える
- 第9章・第12章：GLB検査時の形状メトリクス抽出（頂点数・三角形数・メッシュ数・マテリアル数・テクスチャ枚数と解像度・バウンディングボックス）。ローカルで抽出し、取れない項目はnull
- 固定プリセット（Tripo標準、Tripo HD、Meshy標準など。モデルID・SDK版・パラメーター・価格版・価格確認日・価格根拠URLを含む）
- 第11章の参照価格を版付き管理設定として投入。Meshyの単価はTripoと同じと仮定しない
- 第13章の試験11（外部URI参照GLB拒否）を実アダプターの検査経路でも通す

LIVE_API_ENABLED は false のままにしてください。実APIは呼ばないでください。APIキーがない会社は「モック検証済み・実接続未確認」と表示してください。
THIRD_PARTY_NOTICES.md に使用ライブラリのライセンスを追記してください。

完了したら VERSION を 0.3.0、CHANGELOG.md を更新し、第17章の形式で日本語で報告してください。報告では、各プリセットについて「公式資料で確認済み」「UNVERIFIED」を項目別に明示してください。
```

### A3.5：早期実感触（ブランチ feature/a3-5-early-check）

投入前にユーザーが `.env` にAPIキーを記入し、上限額を決めておく。

```
ブランチ feature/a3-5-early-check を作成し、仕様第10章「早期実感触（A3.5）」を実施してください。

前提（ユーザーが設定済み）：
- .env に TRIPO_API_KEY と MESHY_API_KEY を記入済み（内容をチャットに出さない）
- 全体上限額：__ USD、今回のセット上限額：__ USD
- 試験画像：docs/early-check/ に5枚（題材タグを併記）

範囲：
- LIVE_API_ENABLED=true にする手順と、実行前チェック（キー有無、価格確認日、上限額、同意状態、UNVERIFIED なし）を README と画面に実装
- 検証セット「早期確認」を作り、purpose=benchmark、集計上は「早期確認」として通常検証と分離
- 5題材×2社＝10件を実生成。上限見積が上限額を超えないことを事前に表示
- 完了後、各件について技術状態、待ち時間、見積、形状メトリクス、GLBの読込可否を docs/validation-report.md に記録

禁止：再生成の自動実行、10件を超える送信、APIレスポンス原文やキーのログ出力。

完了したら VERSION を 0.3.5、CHANGELOG.md を更新し、以下を日本語で報告してください：
1. 送信数・技術完了数・失敗数（サービス別）
2. 見積合計と、事業者管理画面で確認した実績（未確認なら「要照合」）
3. 品質の目視所見（これは運営の判断材料であり、合格判定ではないと明記）
4. 発見した不具合と UNVERIFIED の更新
```

### A4：品質比較画面・判定表・限定公開（ブランチ feature/a4-review-ui）

```
ブランチ feature/a4-review-ui を作成し、仕様第16章のA4を実装してください。

範囲：
- 第6.5章：品質比較画面（PC並列／スマホ縦並び、視点操作、基準角度調整、背景色切替、スマホはモデル切替）
- 第6.5章・第7章・第9章：ブラインド評価モード（既定ON、A/B割当をランダム固定して保存、評価確定まで画面・API・URL・ファイル名にサービス名等を出さない、確定後に開示、reveal APIと監査ログ、非ブラインド評価の区別）
- 第6.6章・第10章：校正セット（最初の5件を講師2名が独立評価、2段階以上の差を警告）
- 第6.6章・第10章：集計画面と判定表（サービス×題材タグ、サンプル不足／採用候補／保留／不適、閾値は設定値）。初回合格率と品質のみ初回合格率の併記、2回以内合格率、p50/p95、講師作業時間
- 第11章の費用画面（見積累計、手動確認済み実績、失敗・要照合、上限額との差）、手動実績入力と監査ログ、実績が見積を超えた場合の警告と停止
- 第6.4章のポーリング（詳細5秒、一覧15〜30秒）と、通信確認待ち・受付結果不明・保存失敗の区別表示
- 第13章の試験 12、14、18 と、Playwrightによる主要画面の動作試験
- 第3章・第12章・第15章：deploy/ に Caddyfile 例、限定公開用 compose 上書き、ファイアウォール手順（ダミー値）。READMEに配置手順を追記

スマホレイアウトは iPhone Safari と Android Chrome での実機確認が必要です。端末での確認はユーザーが行うので、確認手順と確認項目のチェックリストを docs/quality-test-plan.md に書いてください。エミュレーションだけで実機合格と報告しないでください。

完了したら VERSION を 0.4.0、CHANGELOG.md を更新し、第17章の形式で日本語で報告してください。
```

### A5：小額実API試験（ブランチ feature/a5-live-test）

投入前にユーザーがVPSに配置し、実機確認を済ませておく。

```
ブランチ feature/a5-live-test を作成し、仕様第16章のA5を実施してください。

前提（ユーザーが設定済み）：
- VPS上で限定公開環境が動作し、実機確認済み
- 全体上限額：__ USD、セット上限額：__ USD
- 試験画像：運営・講師の自作作品20、3D向け準備画像10（題材タグ付き、利用同意は not_required）

範囲：
- 検証セット「初回30題材」を作成し、2社比較で30件×2＝60件を送信
- 不合格の再生成は自動で行わない。講師の評価後、運営の指示があった件のみ retry する
- 完了後、docs/validation-report.md に第17章の項目を記録（送信数、技術完了数、待ち時間p50/p95、見積合計、要照合額、UNVERIFIED、発見した不具合）

禁止：評価の代行、合格判定の記入、架空の数値の記入。品質評価と判定表の最終判断は運営が行います。

完了したら VERSION を 1.0.0、CHANGELOG.md を更新し、第17章の形式で日本語で報告してください。
```

---

## 3. 各ステージ完了時にユーザーが確認すること

- `docs/decisions.md` の BLOCKER に回答し、ASSUMPTION に異議があれば差し戻す
- `docs/provider-contracts.md` の UNVERIFIED が残っていれば、実生成を有効化しない
- 報告の「実装済み／モック検証済み／実API検証済み／未確認」の区別が守られているか
- `.env`、DB、画像、GLB、ログがコミットされていないか（`git status` と `.gitignore`）
- PRをレビューしてから main へマージする。Claude Code に main へ push させない

## 4. 途中で仕様を変えたくなったとき

仕様書 `docs/spec/3d_art_validation_spec_v1_1.md` を先に改訂（付録の改訂一覧に追記）してからコミットし、その後に Claude Code へ「仕様が Ver.1.x に更新された。差分を読んで該当箇所を修正してください」と指示する。チャットで口頭指示だけを積み重ねると、後続ステージで仕様と実装が乖離する。
