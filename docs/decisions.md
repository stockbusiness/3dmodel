# 判断記録（decisions）

正本：`docs/spec/3d_art_validation_spec_v1_1.md`（Ver.1.1 / 2026-09-08）
分類タグの定義は `CLAUDE.md` §2 に従う。

- `BLOCKER`：これが決まらないと次に進めない。ユーザーの入力が必要。
- `ASSUMPTION`：軽微な選択を自分で決めた。後から覆せる形で実装する。
- `DEFERRED`：フェーズB/C、または今回対象外として後送り。
- `SECURITY_REVIEW`：セキュリティ上ユーザーの確認が要る。
- `UNVERIFIED`：外部APIの仕様・モデルID・価格・SDK引数を公式資料で確認できていない。

---

## RESOLVED（ユーザー回答済み。2026-09-08）

### B-1 ブランチ運用の不一致 → 解決

回答：「ブランチは進めやすい方法で大丈夫」。

決定：全ステージをセッション指定の `claude/confirmation-needed-v9f1r7` で進め、ステージごとにコミットを分ける。`CLAUDE.md` §4 の「ステージごとにブランチを切る」は、ステージをまたいで先回りしないための規定と解釈し、その趣旨（1回の作業は1ステージ以下、ステージ完了時に VERSION と CHANGELOG を更新）は維持する。ブランチを分けない代わりに、ステージ境界はコミットと CHANGELOG で識別できるようにする。

### B-2 権限モデル → 解決（案1を採用、将来の分離に備える）

回答：「権限は管理者と講師は同じでいいが、将来的には権限を分ける可能性があります」。

決定：
- 全操作者を同等権限とし、枠追加・上限額変更・手動費用確定・reveal を「理由入力＋監査ログ必須」の操作として全員に開放する（仕様第2章に従う）。
- 将来の分離に備え、`operators.is_admin`（真偽値、既定 true）を A1 の時点で作る。フェーズAでは権限判定に使わず、値の記録のみ。
- 権限判定は `app/auth.py` の単一の関数（例 `require_admin_action()`）に集約し、フェーズAでは「ログイン済みなら許可」を返す実装にする。将来はこの関数の中身だけを `is_admin` 判定に差し替えれば分離できる。呼び出し側に権限判定を散らさない。

関連する SECURITY_REVIEW S-4（講師が上限額を変更できることの許容）は、上記の回答により許容として確定する。抑止は監査ログによる。

### B-3 講師アカウントと校正セット → 解決

回答：「講師アカウントは当面一つでいい」。

決定：
- A1 の初期アカウント作成 CLI は 1 アカウントから作成でき、追加も同じ CLI で行える形にする。
- 校正セット（仕様第10章・第6.6章）は A4 で機能として実装するが、校正対象件数の既定値を 5 件から **0 件**にする。仕様第10章が「最初の5件（設定で変更）」と定めているため、これは設定値の選択であり仕様変更ではない。
- 講師が 2 名になった時点で設定値を 5 に戻せば、2 名の独立評価と 2 段階差の警告がそのまま機能する。
- 1 名運用の間は、集計画面に「校正未実施」と表示し、評価基準のばらつきが検出されていないことを明示する。

---

## 後続ステージで必要になるユーザー入力（現時点では停止しない）

| 必要時期 | 入力 | 備考 |
| --- | --- | --- |
| A3.5 | Tripo / Meshy のAPIキー | `.env` に直接記入。チャットに貼らない |
| A3.5 | 全体上限額とセット上限額（USD） | 未設定時は実API生成を無効（仕様第11章） |
| A3.5 | 早期確認用の試験画像5枚（題材タグ付き） | `docs/early-check/` に配置 |
| A4 | VPSのFQDN、IP制限の許可元、Basic認証情報 | `deploy/` にはダミー値のみ置く |
| A4 | 実機（iPhone Safari / Android Chrome） | 端末確認はユーザーが実施 |
| A5 | 試験画像30枚（運営・講師の自作20、3D向け準備10） | 利用同意は `not_required` |
| A5 | 参考換算レート（USD→JPY）と想定時給（円） | `experiments` の入力必須項目 |

---

## ASSUMPTION（自分で決めた軽微な選択。異議があれば差し戻し可）

| ID | 判断 | 理由 | 影響範囲 | 覆し方 |
| --- | --- | --- | --- | --- |
| A-1 | 依存管理は `pyproject.toml` ＋ `uv.lock`（uv） | 仕様第15章は「依存ロックファイル」としか指定しない。Dockerビルドが速く再現性が高い | ビルド手順 | pip-tools / Poetry へ差し替え可 |
| A-2 | パスワードハッシュは argon2id（argon2-cffi） | 仕様は方式を指定していない。現行の推奨方式 | `app/auth.py` | bcrypt へ差し替え可 |
| A-3 | セッションはDB保存のopaqueトークン（Cookieに乱数、DBにはSHA256のみ保存）。CSRFはフォームトークン＋`SameSite=Lax` | 仕様第6.1章の「セッションのハッシュ・期限」に沿う。JWTは失効管理が増える | `app/auth.py`、`sessions` | — |
| A-4 | 画像のデコード検査・EXIF除去・送信用コピー生成は Pillow | 仕様は方式未指定 | `app/services/image_intake.py` | — |
| A-5 | GLB検査と形状メトリクス抽出は自前のGLBチャンクパーサ＋glTF JSONのaccessor集計。テクスチャ解像度は埋込画像のヘッダから取得 | 仕様第9章「ローカルで抽出し、取れない項目はnull」。trimesh等の重い依存を避ける | `app/services/glb_inspect.py` | pygltflib 等へ差し替え可 |
| A-6 | 題材タグは1アセットにつき1つ | 第10章の判定表が「サービス×題材タグ」のセル単位で、複数タグだと1題材が複数セルに二重計上される | `assets`、判定表 | 複数可にする場合は判定表の分母定義を仕様で決め直す必要あり |
| A-7 | UUIDはv4を36桁の文字列としてSQLiteに保存。金額はmicro-USDのINTEGER | 仕様第9章の指定に沿った具体化 | 全テーブル | — |
| A-8 | 日時はタイムゾーン付きUTCで保存し、画面のみ日本時間に変換 | 仕様第9章 | 全テーブル | — |
| A-9 | `lease_owner` は「ホスト名＋PID＋プロセス起動UUID」 | 再起動後の同一ホスト・同一PID衝突を避ける | `app/worker.py` | — |
| A-10 | 全体上限額は環境変数（例 `GLOBAL_COST_CAP_USD`）、セット上限額は `experiments` の列 | 仕様第11章「セット単位と全体（環境設定）」の具体化 | `app/config.py`、`cost_guard` | — |
| A-11 | CSVの数式エスケープは、先頭が `=` `+` `-` `@` タブ・CR のセルに `'` を前置 | 仕様第6.6章「表計算での数式実行を防ぐエスケープ」の具体化 | `csv_export.py` | — |
| A-12 | `VERSION` と `CHANGELOG.md` を成果物に追加する | 仕様第15章の一覧には無いが `CLAUDE.md` §4 が要求。矛盾はしない | リポジトリ直下 | — |
| A-13 | `docs/plan.md` と `docs/decisions.md` を追加する | 仕様第15章の一覧には無いが `CLAUDE.md` §1・§3 が要求 | `docs/` | — |
| A-14 | 生成受付の `purpose` の既定値は `benchmark` | 第7章は2値（benchmark / classroom_simulation）を定めるが既定を書いていない。フェーズAは検証が主目的 | 受付API | — |
| A-15 | 校正対象件数の既定値を 0 件にする | B-3 の回答（講師1名）。仕様第10章は「最初の5件（設定で変更）」と定めており、設定値の選択にあたる | A4 の校正セット | 設定値を 5 に戻せば復帰 |
| A-16 | `operators.is_admin` を A1 で作るが、フェーズAでは権限判定に使わない | B-2 の回答（将来分ける可能性あり）。後から列を追加するマイグレーションを避ける | `operators`、`app/auth.py` | 判定関数の中身の差し替えのみで分離可能 |
| A-17 | ステージごとにブランチを切らず、単一ブランチでコミットを分ける | B-1 の回答（進めやすい方法でよい） | Git運用 | — |
| A-18 | 形状メトリクスの抽出を A3 ではなく A1 で実装した | 仕様第12章の GLB 検査（外部URI参照の拒否＝必須試験11）と同じ glTF パース処理であり、分けると同じパーサを2度書くことになる。依存も増えない | `app/services/glb_inspect.py` | — |
| A-19 | 利用同意 `missing` は「実生成（外部送信）」を拒否し、モック生成は許可する | 仕様第12章の禁止対象は外部送信。モックは外部通信を行わない。画面には未取得の警告を出す | `check_can_submit` | 全面禁止にする場合は1行の変更 |
| A-20 | 生成結果画面の model-viewer は `loading="eager"` | この画面はモデル1件のみ。遅延読込だと画面外で読み込みが始まらず、読込状態の表示が実態とずれる。2件を並べる比較画面（A4）では仕様第6.5章に従い遅延読込・切替にする | `generation_detail.html` | — |
| A-21 | セッションCookieの `secure` 属性はリクエストが HTTPS のときのみ付ける | ローカル開発は http、限定公開環境（A4）は Caddy による HTTPS。固定すると片方で動かない | `app/routes/auth_routes.py` | — |
| A-22 | 送信枠の追加を `quota_grants` 表で持つ | 仕様第9章は「枠の追加は管理者の理由付き監査操作のみ」と定めるが保存場所を指定していない。(検証セット, 題材, サービス) の組で数える必要があるため専用の表を作った。監査ログにも残す | `app/services/quota.py` | — |
| A-23 | セット上限額は実/モックを問わず効き、全体上限額は実API生成にだけ効く | セット上限額をモックでも効かせないと、上限額の挙動をモックで検証できない。モックは実費が発生しないため全体上限は消費させない | `app/services/cost_guard.py` | — |
| A-24 | 日時列に `UtcDateTime` 型を使い、読み出し時にUTCを付け直す | SQLite は日時にタイムゾーンを保存しないため、素の `DateTime` では naive な値が返り aware な値と比較・減算できない（A2で実際に不具合になった） | `app/models/tables.py` の全日時列 | — |
| A-25 | 生成結果画面の5秒ポーリングを A2 で実装した | A2で生成が非同期になったため、これが無いと受付後に状態が更新されず画面が使えない。一覧の15〜30秒ポーリングと第6.4章の残りは計画どおり A4 | `app/static/generation.js` | — |
| A-26 | 429 を送信時に受けたら `queued` に戻して待つ | 429 は事業者が要求を受け付けていないことを意味するため、再送しても二重課金にならない。状態確認時の429はバックオフのみ（状態は running のまま） | `app/services/worker_steps.py` | — |
| A-27 | Tripo の `model_version` は SDK の既定値 `v2.5-20250123` を採用 | より新しい版（`v3.1-20260211` 等）もあるが、品質と価格の差を公式資料で確認できていない。推測で新しい版を選ばない | `tripo-standard` プリセット | 価格確認後に見直す（UNVERIFIED U-10） |
| A-28 | Meshy の `should_texture` を true にする（APIの既定は false） | 既定はテクスチャ無しの下書きメッシュで、教室での品質評価に使えない。**課金が増える側の選択**であることを記録する | `meshy-standard` プリセット | false に戻せば下書きのみ |
| A-29 | Meshy へ画像を `data:` URI で送る | 公式CLIも同じ方法をとる。こちらの画像を公開URLに置かずに済み、仕様第12章に沿う。10MiBの画像で本文が約13MBになる点は許容する | `app/providers/meshy.py` | — |
| A-30 | 成果物の配信ホストは設定で与え、既定は空にする | 配信ホストを公式資料で確認できていない。空のあいだは何も取得しないので、推測のホストを許可してしまうことがない | `download_guard`、`.env` | ホスト確認後に設定 |
| A-31 | Tripo公式SDKの `TRIPO_DISABLE_GEO_DETECTION` を `setdefault` ではなく上書きで設定する | `"0"` を明示されると位置検出が動いてしまう。外部への接続は仕様第12章で限定している | `app/providers/tripo.py` | — |
| A-32 | 比較の開示は「全ての結果の評価が確定したとき」に自動で行う。技術失敗で評価に至らない結果は待たない | 仕様第6.5章は「評価が確定するまで」としか定めていない。失敗した結果を待つと、いつまでも開示できない比較が残る | `app/services/blind.py` | — |
| A-33 | 校正対象の集計に使う評価者を `experiments.calibration_reviewer_id` で持つ | 仕様第10章は「後から運営が指定した1名分を使う」と定めるが保存場所を指定していない。検証セット単位の設定として持たせた | `experiments` | — |
| A-34 | 基準角度（正面の向き）は端末の localStorage に保存する | 仕様第6.5章は「講師が基準角度を調整できる」までしか定めていない。サーバーに保存すると講師どうしで上書きし合う。読めない環境でも表示は壊さない | `app/static/compare.js` | サーバー保存にする場合は比較単位の列を足す |
| A-35 | 比較画面のビューアーは遅延読込にする | 仕様第6.5章「重いGLBを同時に複数読み込まず」。単体の生成結果画面はモデル1件なので即時読込（A-20） | `comparison_detail.html` | — |
| A-36 | 判定表のセルは「初回送信題材数」を分母にする | 仕様第10章の各比率が「初回送信題材数」を分母と定めている。生成の件数ではなく題材の件数で数える | `app/services/verdict_table.py` | — |
| A-38 | Meshy の見積単価に、確認できた中で**最も高い購入経路**（追加クレジットパック $10/250credits ＝ $0.04/credit）を採り、$1.20/件とする | 単価は購入経路で変わる（定額 Pro なら $0.02/credit）。`price_max_micro_usd` は仕様第11章の「上限側の見積」なので最大値が正しい。どの経路で支払っても見積が実費を下回らない | `app/services/presets.py`、`meshy-standard` | 契約プランが決まったら、その経路の単価に下げてよい（`price_version` を上げる） |
| A-39 | 価格が確認できても、プリセットは `is_enabled=False` のままにする | 仕様第4章はデータ取扱い条件も確認対象としている。価格だけで実生成を開けない。`is_unverified` は価格・モデルIDの確認状態を表す旗なので、そちらは False にした | `app/services/presets.py`、`selectable_reasons` | U-4a/U-4b/U-4c が解消したら有効化 |
| A-37 | Meshy の取消に対応したうえで、取り消しても送信枠は返却せず保持する | 公式資料で `DELETE` が実行中タスクの取消だと確認できた（`PENDING` は返却、`IN_PROGRESS` は返却なし）。こちらからは取消時点の状態を確実に判定できないため、返却されない前提（＝上限額に対して安全側）に倒す。仕様第8章の「送信後の取消は枠を保持」にも一致する | `app/providers/meshy.py`、`app/services/generation.py` | 事業者側の残高で返却を確認できたら、照合機能で戻す |

---

## SECURITY_REVIEW（ユーザーの確認が要る）

| ID | 内容 | 確認事項 |
| --- | --- | --- |
| S-1 | 初期管理者作成CLIのパスワード入力方法 | 対話入力（画面非表示）を既定とし、標準出力・引数・環境変数に平文を出さない方針でよいか。仕様第6.1章は「固定値禁止」までしか定めない |
| S-2 | 限定公開環境の前段認証（第3章・第12章） | IP制限とBasic認証のどちらを採るか、両方か。Basic認証情報の受け渡し経路（チャットに貼らない） |
| S-3 | APIキーの投入経路 | `.env` への直接記入をユーザーが行い、エージェントは内容を読まない・出力しない運用でよいか |
| S-4 | 権限モデル（B-2）の帰結 | **確定済み**：ユーザー回答により案1を採用。講師アカウントが上限額・送信枠を変更できることを許容し、抑止は理由入力と監査ログによる |
| S-5 | CSP を2点だけ緩めている | model-viewer を動かすために `script-src` に `'wasm-unsafe-eval'`、`style-src` に `'unsafe-inline'` が必要。いずれも外部への接続は許しておらず、スクリプトの inline も許していない。これで問題ないか確認をお願いしたい。厳格化する場合は、model-viewer を諦めるか、テクスチャ・WebAssemblyを使わない自前ビューアーを作ることになる |

---

## A4で追加した安全側の実装

| 内容 | 根拠 |
| --- | --- |
| ブラインド中は、画面HTMLだけでなく **APIレスポンス・配信URL・Content-Disposition のファイル名** にもサービス名等を出さない | 仕様第6.5章・第12章。試験18で4つの経路すべてを確認している |
| 手動開示は理由が必須で、監査ログに残る | 仕様第7章。評価前の開示は結果の見方に影響するため |
| 確定実費が見積合計を超えたら、新規生成を受け付けない | 仕様第11章「警告を出し、新規生成を停止する」 |
| 校正対象は、2名そろわない、または採用する評価者が未指定のあいだ、集計から**除外**する（平均は使わない） | 仕様第10章 |

---

## A3で判明した、事業者SDKの扱いに関する重要事項

`docs/provider-contracts.md` に詳細を記録した。特に次の2点は、そのまま使うと
仕様第12章に反するため、こちらで対処している。

| 内容 | 対処 |
| --- | --- |
| Tripo公式SDKは、成果物のダウンロードでSSL証明書の検証に失敗すると、**検証を無効にして取得し直す**（`verify_mode = ssl.CERT_NONE`） | SDKのダウンロード経路を使わず、`app/services/download_guard.py` で自前に取得する |
| Tripo公式SDKは import 時にバックグラウンドで第三者のIP位置情報サービスへ問い合わせる（`ip-api.com` と `ipinfo.io` は**平文HTTP**） | アダプターが import より前に `TRIPO_DISABLE_GEO_DETECTION=1` を必ず設定する。Dockerfile にも設定する |

---

## A2で追加した安全側の実装（仕様に明記はないが趣旨に沿うもの）

| 内容 | 根拠 |
| --- | --- |
| 外部送信の直前にも利用同意を確認する | 仕様第12章は「サーバー側でも拒否する（UIの非活性だけに頼らない）」と定める。受付のあとで同意が取り下げられる場合があるため、ワーカーが送信する直前にも確認し、未取得なら送信せずに中止して枠を返す |
| 冪等キーに署名を付け、サーバー発行のものだけを受け付ける | 仕様第6.3章の「サーバーが画面描画時に発行してフォームに埋め込む」を、API直叩きでも守れる形にした。APIは `GET /api/form-token` で取得する |
| 受付の入口で BEGIN IMMEDIATE を取る | SQLite の遅延トランザクションは読み取りから書き込みへ昇格するときに即座に SQLITE_BUSY になり、busy timeout で待てない。同時受付の試験（第13章 試験3）で実際に発生した |

---

## 未確認（この環境の制約により検証できていないもの）

| ID | 内容 | 状況 |
| --- | --- | --- |
| E-1 | `docker compose build` と `docker compose up` の実動作 | 開発に使ったサンドボックスのネットワーク方針が、コンテナイメージの blob 配信元（`production.cloudfront.docker.com`、`pkg-containers.githubusercontent.com`）への接続を拒否するため、ベースイメージ `python:3.11-slim-bookworm` を取得できずビルドを実行できていない。Dockerfile と compose.yaml は記述済みで、依存の導入手順（`uv sync --frozen --no-install-project --no-dev`）はローカルで同じコマンドを実行して成功を確認した。**ユーザーの環境で `docker compose build` を一度実行して確認をお願いしたい。** |
| E-2 | 実機（iPhone Safari / Android Chrome）での表示 | 端末がないため未確認。ブラウザーのエミュレーションだけで実機合格とはしない（仕様第13章）。確認手順とチェックリストは `docs/quality-test-plan.md` にまとめた。限定公開環境（`deploy/`）に配置してから実施する |

なお、画面と3D表示は Chromium（Playwright）で、PC幅（1280px）とスマホ幅（390px）の
両方について、モデルの読込完了・読込失敗時の表示・ブラインドの秘匿と開示・
スマホでの縦並びと切替・ボタンの高さ44px以上・本文16px・横スクロールが無いことを
自動試験（`tests/test_screens_playwright.py`）で確認している。
**これは実機確認の代わりにはならない。**

---

## DEFERRED（今回対象外）

| ID | 内容 | 根拠 |
| --- | --- | --- |
| D-1 | 参加者登録、LINEログイン・通知、課金、予約、利用権連携、代理店管理、公開共有URL、展示空間、アバター、AR、Blender接続、GPU自社運用、自動品質採点、AI画像生成・自動背景除去 | 仕様第2章 |
| D-2 | 費用の予約／確定／解除ライフサイクル、未確定額の相殺、為替の自動取得 | 仕様第2章・第11章でフェーズBへ移動。Aは上限額・受付判定・手動確認実績の3点のみ |
| D-3 | PostgreSQL・オブジェクトストレージ移行の実装 | 仕様第3章（境界は作るが移行実装は不要） |
| D-4 | データ削除機能 | 仕様第9章（バックアップからの復元手順で代替） |
| D-5 | LINE内ブラウザでの実機確認 | 仕様第13章（後続） |
| D-6 | **U-15（Meshy の Free プランでAPIキーを発行できるか）の確認を保留**（2026-09-08、利用者の判断） | 契約プラン（Free のままか Pro $20/月か）の判断とセットになる項目で、先に規約側（U-4b/U-4c：学習利用の可否・生成物の利用条件）を片付けるほうが順序として自然。**A3.5 で Meshy を実際に動かす直前に再確認する**。それまで `meshy-standard` は無効のままなので、保留にしても誤って実生成が走ることはない |

---

## UNVERIFIED（A3で公式資料により確認する。それまで実生成を有効化しない）

| ID | 内容 | 確認先（仕様第4章） |
| --- | --- | --- |
| U-1 | ~~Tripo公式SDKの採用タグ/コミット、モデルID、パラメーター、返却形式、エラー、タイムアウト、作成系の自動リトライ挙動~~ → **確認済み（2026-09-08）**：`tripo3d` 0.4.2、タグ `v0.4.2`。詳細は `docs/provider-contracts.md` | tripo-python-sdk |
| U-2 | ~~Meshy image-to-3D の公式契約（エンドポイント、パラメーター、状態値、成果物取得方法、取消）~~ → **確認済み（2026-09-08）**：公式CLI `meshy-cli` 0.2.0 のソースと、公式ドキュメント一括配布 `docs.meshy.ai/llms-full.txt` による | meshy-dev/meshy-cli、docs.meshy.ai |
| U-7 | 両社：成果物を配信するホスト名。**Meshy は確認済み（2026-09-08）：`assets.meshy.ai`**。Tripo は未確認で、`APP_TRIPO_DOWNLOAD_HOSTS` が空のあいだは取得しない | 実物のURL、または公式資料 |
| U-8 | 両社：レート制限の具体的な数値。**Meshy は確認済み（2026-09-08）：最も低いプランでも 20 req/s・同時10件で、こちらの上限（全体2・各社1）を十分下回る**。Tripo は未確認 | 公式資料 |
| U-9 | ~~Meshy：`DELETE /image-to-3d/{id}` が実行中タスクの取消になるか、課金はどうなるか~~ → **確認済み（2026-09-08）**：実行中タスクの取消。`PENDING` は返却、`IN_PROGRESS` は返却なし、終了済みは取消不可。`supports_cancel = True` にした（A-37） | docs.meshy.ai |
| U-10 | Tripo：`model_version` をより新しい版にすべきか（品質と価格の差） | 公式資料 |
| U-11 | Tripo：送信時の `{"type": "jpg"}` 固定申告が PNG/WebP 入力の結果に影響するか | 公式資料 |
| U-3 | ~~価格~~ → **確認済み（2026-09-08）**：Tripo は 30credits × $0.01 ＝ **$0.30/件**。Meshy は 30credits × $0.04（上限側＝追加クレジットパック $10/250credits）＝ **$1.20/件**。プリセットに反映済み（A-38） | 各pricingページ、Meshyの購入画面（USD表示） |
| U-4 | 送信画像・生成物の事業者側での保持期間、学習利用の可否とopt-out手段、生成物の利用条件。**Meshy は確認済みが2点（2026-09-08）：生成物の保持は最大3日、生成物のライセンスは Free = CC BY 4.0／Pro以上 = Private license**。送信画像の保持期間、学習利用とopt-out、ライセンス条文、Tripo 側は未確認 | 各社の規約 |
| U-5 | ~~同梱する model-viewer の固定バージョンとライセンス~~ → **確認済み**：4.3.1、Apache-2.0。npm から取得し SHA256 を `THIRD_PARTY_NOTICES.md` に記録（2026-09-08） | github.com/google/model-viewer |
| U-6 | Tripo / Meshy が返すGLBが Draco 圧縮（`KHR_draco_mesh_compression`）や KTX2 テクスチャ（`KHR_texture_basisu`）を使うか。使う場合、`app/static/vendor/draco/` と `ktx2/` にデコーダーを同梱する必要がある。同梱するまで圧縮GLBはビューアーで読込エラーになる（外部の gstatic へは取りに行かせない設定済み） | 各社の公式資料と、A3.5で得た実物のGLB |

---

## 記録：Ver.1.1受領前に検出し、Ver.1.1で解消した事項

2026-09-08、リポジトリに置かれていた仕様がVer.1.0のみで、`CLAUDE.md` と実装指示書が参照するVer.1.1が存在しなかった。以下はVer.1.0に規定が無く、Ver.1.1で追加されたことを確認済み：利用同意、ブラインド評価、`resolve-artifact`、A3.5早期実感触、判定表、状態遷移表、ワーカーの処理単位、形状メトリクス、校正セット、品質のみ初回合格率、一覧ポーリング15〜30秒、`deploy/`（限定公開環境）、加工種別の選択式化、事業者のデータ取扱い条件、必須試験16〜20。

Ver.1.1を `docs/spec/3d_art_validation_spec_v1_1.md` として配置し、以降これを唯一の正本とする。
