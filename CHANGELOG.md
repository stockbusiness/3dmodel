# 変更履歴

日付は日本時間。版はステージ完了時に上げる（`CLAUDE.md` §4）。

## 未リリース

### 記録（実装はしていない）：切り口を「メタバースで使える3D」に変える件

利用者から方針が示された。**AIアートの延長ではなく「作った3Dがメタバースで使える」
という切り口で集客する。**

- **この転換は仕様 Ver.1.1 が想定していたもの**である。改訂記録 第13項は形状メトリクスを
  記録する理由を「**千ノ国側で再利用する判断が出た際の再検証を省ける**」と書いている
- **計測はすでに動いている。** `glb_inspect.py` が頂点数・三角形数・メッシュ数・
  マテリアル数・テクスチャ枚数と解像度・バウンディングボックスを抽出済み。
  `tripo.py` も `face_limit` 等を透過的に渡せる。**足りないのは判定基準だけ**
- **仕様と4箇所で衝突する**（第1章「Unity適合性は判定しない」、第9章「メトリクスは
  判定に使わない」、第10章の評価軸5つ、第6.5章のメトリクス表示時期）。
  **修正提案として記録し、仕様は変更していない**（CLAUDE.md 第1章）
- **権利が懸案から要件に昇格した。** Tripo 無料は生成物の権利を会社が保持、
  Meshy Free は CC BY 4.0。**「メタバースで使える」を売りにするなら有料プラン必須**で、
  無料前提の T-11 とは両立しない
- Tripo SDK に `convert_model`（GLTF/USDZ/FBX/OBJ/STL/3MF、`face_limit`・
  `texture_size`・`scale_factor`・`pivot_to_center_bottom`・`flatten_bottom`）、
  `rig_model`、`smart_lowpoly` 等が**実在することをソースで確認**した。
  **価格は未確認のため実装しない**（U-27〜U-30）
- **判明した制約：`rig_model` の仕様に VRM が無い**（`tripo` と `mixamo` のみ）。
  **アバターより置物のほうが現実的**
- **BLOCKER を5件立てた**（B-4〜B-8）。**対象プラットフォームが決まらないと、
  形式・上限値・合格基準が一つも決まらない**
- `docs/decisions.md` T-17、`docs/provider-contracts.md` 第1.11節・U-27〜U-30

### 記録（実装はしていない）：教室のAIアートと画像→3Dの要求が正反対である件

利用者から教室のAIアートの実物5点が示され、「そもそも意味があるのか」という問いがあった。
**この問いは仕様第16章 A3.5（早期実感触）の目的そのもの**である。

- **絵として完成しているほど画像→3D には向かない**という構造的な不一致を整理した
  （映える構図・背景込み・被写界深度 ⇔ 全身・無地背景・明確な輪郭）
- 仕様第10章の評価軸「**全周の形状**」と突き合わせると、**風景画は原理的に不合格**になり、
  胸から上で切れた絵は「元画像の特徴」と「全周の形状」が両立しない
- 5点それぞれの結果を**予想として**記録した。**未検証であり実測値ではない**
- **A3.5 の5枚を「そのまま」と「3D向けに整えたもの」の対比にする案**を提示した。
  題材選定の変更であり仕様の変更ではない（仕様第9章の分類に「3D向けに準備」が既にある）。
  **採否は利用者の判断待ち**
- 権利の確認事項を記録：生徒作品でないことの確認、第三者商標の写り込み
- `docs/decisions.md` T-16

### 記録（実装はしていない）：教室の題材が多視点1枚である件

利用者から、教室で3D化したい画像の例として
**正面図と背面図を1枚に並べたもの**（ターンアラウンド）が示された。

- 現行の `tripo-standard` は Tripo の `image_to_model`（**1枚＝1つの対象**）を呼ぶため、
  この形の入力は**2体が並んだモデルになる可能性が高い**
- 対応するAPI `multiview_to_model` は公式SDKに実在することを**ソースで確認した**が、
  **1件あたりの価格と、画像配列の各位置が示す向きが未確認**のため
  **実装しない**（U-26。CLAUDE.md 第2章）
- A3.5 では**正面1枚に切り出して**送る。並んだまま送ると、失敗したときに
  「品質が低い」のか「入力の形が合っていない」のか切り分けられなくなるため
- `docs/decisions.md` T-15、`docs/provider-contracts.md` 第1.10節・U-26、
  `docs/early-check-plan.md` 第2.1節に記録した
- あわせて、早期確認の**画像選定条件**（背景・大きさ・解像度・易しい順に並べる）を
  runbook に明文化した

### 確認できたこと（利用者が両社のAPI設定画面を提示）

- **Meshy：無料プランではAPIキーを発行できない**（U-15 解消）。
  設定の「API」タブが有料プランへの案内で覆われており、キーの一覧も発行操作も使えない。
  本システムはAPI経由でしか動かないため、**Meshy を A3.5 に参加させるには
  Pro（$20/月）以上の契約が要る**（`docs/decisions.md` T-12）
  - あわせて、**$10 の追加クレジットパックで無料枠の不足を補う案は成立しなくなった**。
    クレジットを買ってもAPIが使えるようにはならない
- **Tripo：無料アカウントでもAPIキーを発行できる**（U-23 の一部）。
  キーは**作成時に1回だけ表示される**。**IP制限**の機能がある（`docs/decisions.md` S-7）。
  事業者自身が「環境変数で保存し、バージョン管理にコミットしない」ことを案内しており、
  本システムの方針（S-6）と一致する
  - 残るのは**無料アカウントのクレジット残高**。キーを設定して管理画面の接続テストを
    実行すれば分かる

### 運営の判断

**T-11（当面は無料）と「A3.5 で2社を比較する」ことが両立しない**ことが確定したため
選択肢を並べ、**案A（Tripo だけで A3.5 を先行実施）が選ばれた**（`docs/decisions.md` T-13）。

- 5題材 × 1社 ＝ **5件**、見積上限 **$1.50**
- **2社比較にはならない。事業者選定の材料にはならない**点は記録に明記した

### 追加（A3.5 の準備）

- **`docs/early-check-plan.md`**：A3.5 の実施手順書。事前に用意するもの、
  題材の選び方（**Tripo の無料は送信画像の権利も会社が保持するため生徒作品は使わない**）、
  配信ホストの確認手順、結果の見方、止まったときの対処
- **`docs/early-check/README.md`** と `.gitignore`：試験用画像の置き場。画像はコミットしない
- **`tripo-standard` を有効にした**（`meshy-standard` は無効のまま）。
  有効にしても実生成にはならない（`LIVE_API_ENABLED`・上限額・利用同意・送信枠が別途必要）
- `apply_preset_availability()` を追加し、`init-db` / `seed-presets` から呼ぶ

### 追加（A3.5 を運営の環境で実行するため）

- **`python -m app.cli check-provider <事業者> [--connect]`** を追加。
  画面を開かずに端末で設定と認証を確かめられる。**APIキーの値は表示しない**
- **開発サンドボックスから事業者のAPIへ接続できないことを確認し記録した**
  （`docs/decisions.md` **E-2**）。`api.tripo3d.ai` / `api.meshy.ai` /
  `assets.meshy.ai` はいずれも egress proxy が CONNECT に 403 を返す。
  **A3.5・A5 の実生成は運営の環境で実行する**

### 修正

- **`compose.yaml` で `TRIPO_API_KEY` / `MESHY_API_KEY` を `web` にも渡すようにした。**
  管理画面の接続テストと `app.cli check-provider --connect` は `web` で動くため、
  worker にしか渡していないと**`.env` に書いても「未設定」と表示されていた**。
  両サービスに渡っていることを試験で守るようにした（`tests/test_admin.py`）
- `pyproject.toml` の dev に `pyyaml` を明記（compose の配線試験で使う。間接依存に頼らない）

### 変更（A3.5 の詰みを解消）

- **保存失敗の理由を5秒ごとの巡回でも画面に反映するようにした。**
  配信ホストの確認（手順書 3.3）は拒否メッセージのホスト名を読む手順なので、
  読み込み直さずに読めないと手順が止まる。試験で守るようにした


- **配信ホスト未設定を「実生成の選択不可」の理由から外した**（`docs/decisions.md` A-44）。
  仕様第6.3章の一覧に無いうえ、**ここで送信を止めると「配信ホストを確認するための1件」
  すら出せない**という詰みになっていた。実際の防御は `download_guard` が行う
- **許可ホスト未設定の拒否メッセージに、そのURLのホスト名を添えた**（同 A-45）。
  拒否そのものは変えていない

---

## 未リリース（A3.5 の実施準備）

### 確認できたこと（利用者の環境で実際に動かした結果）

- **`docker compose build` が成功した**（Windows 11 + Docker Desktop）。
  開発サンドボックスからコンテナレジストリへ接続できず未確認だった **E-1 が解消**
- **`docker compose run` による単発実行も成功**。`init-db` で Alembic の移行4本が
  新規DBへ順に適用され、プリセット13件が投入された
- **Tripo の APIキーで認証が通った**（公式SDKの `get_balance()`。生成は行っていないので
  課金なし）。**ただしクレジット残高は 0**
- **U-23 解消。結論は「無料では生成できない」。**
  Tripo はキーを発行でき認証も通るが残高0、Meshy はキーそのものを発行できない。
  **T-11（当面は無料）と A3.5 の実施は両立しない**ことが確定した
  （`docs/decisions.md` **T-14**。運営の判断待ち）
- **単価が実取引で確定（2026-09-09）**：$5 の購入で **500credits** が付与された。
  **$0.01/credit** で価格ページの記載と一致。したがって `tripo-standard` の
  見積 **$0.30/件（300,000 micro-USD）はそのまま有効**。
  第1.4節の根拠を「価格ページの記載」から「実取引で確認」に格上げした
- **U-25 も同日に解消**：Tripo のクレジット購入は**最小 $1・1〜100000 の整数 USD**。
  **A3.5 に必要なのは $2**（150credits に $1＝100credits では足りない）。
  購入画面は**既定で $50 が選ばれている**ため、カスタム金額の入力が要る（第1.9節）

### 追加（安全側）

- **`.env.example` の例示用 `APP_SECRET_KEY` のままでは起動しないようにした。**
  `.env.example` を写しただけで動いてしまうと、セッションCookieの署名鍵が
  公開の既知値のまま運用に入る。起動時に弾いて作り直しを促す（仕様第6.1章「固定値禁止」）

### 修正（A3.5 の前に見つけた不具合・U-11 解消）

- **PNG / WebP を Tripo へ送ると「JPEG」として申告されていた。**
  公式SDK 0.4.2 の `upload_file` は**ファイル名の拡張子**から申告形式を決めるが
  （`_EXT_TO_STS_FORMAT`。不明な拡張子は `"jpeg"`）、こちらの保存キーは
  拡張子を持たない（仕様第12章：元ファイル名を保存パスに使わない）ためだった。
  **正しい拡張子を付けた一時ファイル経由で渡すようにした**（`docs/decisions.md` A-46）。
  申告できない形式は推測で送らずに拒否する。一時ファイルは無作為な名前で必ず後始末する
  - これまで **U-11 として未確認だった項目の正体**であり、A3.5 で PNG を使うと
    結果が読めなくなるところだった

### 追加（設定不備を読めるようにする）

- **CLI が設定の不備を日本語で説明するようにした。** `.env` の説明文を
  置き換え忘れたときに pydantic の追跡情報だけが出て原因が分からなかったため、
  どの項目が悪いか・何をすればよいかを出す。**入れられていた値そのものは出さない**
- 手順書 3.1 を**手で写さない形**に変えた。署名鍵はコマンドで直接書き換え、
  置き換え漏れを見つける確認コマンドも添えた

### 追加（Windows 向けの手順）

- 手順書に Windows（PowerShell）と Docker を使わない場合を追記。
  **`.env` から読まれるのは `APP_` で始まる設定だけ**で、`TRIPO_API_KEY` は
  シェルの環境変数として設定する必要があることを明記した
  （Docker Compose 経由なら `.env` から読まれるのでこの問題は起きない）

---

## 0.5.0 — 2026-09-08（管理画面）

事業者接続の確認・接続テストと、成果物の確認・管理を1画面にまとめた。
仕様第16章のステージ外だが、A3.5 の準備（APIキーの設定確認）を画面から行えるようにするため。

### 追加

- **管理画面**（`/admin`）
  - **事業者接続の自己診断**（外部通信なし）：APIキーが設定されているか、
    先頭が想定どおりか、成果物の配信ホストが設定されているか、
    実API生成が有効か、全体上限額が設定されているか
  - **APIキーの設定手順の案内**：`.env` に書く行の見本と、再起動が要ることの明示
  - **接続テスト**：認証が通るかだけを確かめる。
    Tripo は公式SDKの `get_balance()`、Meshy は公式CLIが使う一覧取得を用いる。
    **どちらも生成を行わないので課金は発生しない**（`docs/decisions.md` A-40）
  - **プリセットの一覧**：見積額（上限側）、価格の版と確認日、実生成に選べない理由
  - **運用の状態**：`LIVE_API_ENABLED`、全体上限額、同時外部タスクの上限
  - **保存領域の点検**：記録はあるのに実物が無いもの（欠落）と、
    実物はあるのに参照されていないもの（孤立）を報告する
- **成果物の管理画面**（`/admin/artifacts`）
  - 検証セット・保存検査NGでの絞り込み、ダウンロード、生成の詳細への導線
  - **保存物の照合**：DBの記録どおりのファイルが実在し、大きさとSHA256が一致するか
  - 件数は**実APIとモックを分けて表示**する（混ぜて数えない）
- API：`GET /api/admin/providers`、`POST /api/admin/providers/{provider}/connection-test`、
  `GET /api/admin/artifacts`、`POST /api/admin/artifacts/{id}/verify`、`GET /api/admin/storage`

### 変更

- **キー未設定・配信ホスト未設定を、実生成の選択不可の理由に加えた**。
  仕様第6.3章が「キー未設定…は実生成を選択不可とする」と定めているのに、
  これまで判定していなかった（`app/services/presets.py`）
- ヘッダーに「管理」へのリンクを追加

### 守っていること

- **APIキーの値を画面にもAPIにもログにも出さない。**
  値・先頭数文字・長さ・ハッシュのいずれも返さない（仕様第12章）
- **画面からAPIキーを保存できない。** キーは環境変数からのみ読む
  （`CLAUDE.md` 第5章。方針の確認をお願いしたい：`docs/decisions.md` S-6）
- **接続テストは `LIVE_API_ENABLED=true` のときだけ実行でき、監査ログに残る**（A-41）
- **ブラインド評価中の成果物はサービス名を伏せる。** 管理画面から漏れないよう
  一覧も `blind.public_generation` を通す（A-43）
- **削除機能を持たない**（仕様第9章・D-4。A-42）

### 検証

- 自動試験 **123件すべて成功**（管理画面の17件を追加）
- Playwright で管理画面・成果物管理画面の描画とコンソールエラー無しを確認。
  保存領域の点検を実際に実行し、ブラインド中はサービス名が出ないことを確認
- 実APIは呼んでいない（`LIVE_API_ENABLED=false`）

---

## 未リリース（A3.5の前提を1つずつ確認中）

ステージではなく、A3.5（早期実感触）を止めている未確認事項を潰す作業。
版は上げない。

### 確認できたこと（利用者が公式ページを開いて提示）

- **Tripo の価格**：1credit = $0.01、画像→3D（テクスチャ付き）30credits ＝ **$0.30/件**
- **Meshy のクレジット数**：画像→3D（テクスチャ付き）**30credits**。USD単価は未確認のまま
- **Meshy の取消**：`DELETE /image-to-3d/{id}` は実行中タスクの取消。
  `PENDING` は返却、`IN_PROGRESS` は返却なし、終了済みは取消不可
- **Meshy のレート制限**：最も低いプランでも 20 req/s・同時10件
  （こちらの上限は全体2件・各社1件なので十分下回る）
- **Meshy の成果物**：`assets.meshy.ai` の署名付きURL。**保持は最大3日**
- **Meshy の失敗時のクレジット**：`FAILED` のタスクは `consumed_credits` が 0（自動返却）
- **Meshy のプラン価格（USD）**：Pro $20/月＝1,000クレジット、Premium $40/月＝3,000、
  Ultra $100/月＝8,000、Free $0＝100
- **Meshy の追加クレジットパック**：$10/250、$32/1,000、$84/3,000（＝ $0.04〜$0.028/credit）
- **Meshy の生成物のライセンス**：Free は **CC BY 4.0**（クレジット表記で商用利用可）、
  Pro以上は Private license for all assets（条文そのものは未取得）

### 変更（価格の反映）

- **`tripo-standard` に $0.30/件（300,000 micro-USD）を設定**
- **`meshy-standard` に $1.20/件（1,200,000 micro-USD）を設定**。
  単価は購入経路で変わるため、確認できた中で最も高い経路
  （追加クレジットパック $10/250credits ＝ $0.04/credit）を採った。
  仕様第11章「見積は常に上限側を採る」に従う（`docs/decisions.md` A-38）
- 両プリセットの `is_unverified` を False にした。
  **`is_enabled` は False のまま**（データ取扱い条件が未確認のため。A-39）
- `apply_confirmed_prices()` を追加し、`init-db` / `seed-presets` から呼ぶようにした。
  既に価格が入っている行は書き換えない

### 変更

- Meshy アダプターの取消に対応した（`supports_cancel = True`、`cancel()` 実装）。
  返却の有無をこちらから判定できないため、**取消しても送信枠は保持する**（安全側）

### 確認できたこと（Tripo の規約本文・利用者が提示）

- **無料と有料で権利が正反対**。無料利用者が入力した画像と生成物は、
  Tripo が利用・改変・公開・再許諾・収益化の権利と知的財産権を保持する（第5.2.1条）。
  有料利用者は自ら権利を持ち、**「学習データには使わない」と明記**されている（第5.2.2条）。
  **教室での利用は有料が前提**（`docs/decisions.md` T-1）
- 生成物は商用・非商用とも利用可。競合モデル・サービスの作成のみ禁止（第3.2条）
- 保管義務が無く保持期間の定めも無い。「公開」領域のものは永久保持されうる（第4条・第10.5条）
- **「選ばなければ最も公開度の高い設定になりうる」**（第4条）。
  API経由の資産の既定の公開範囲は未確認（U-17）
- 13歳未満の個人データの送信には保護者・後見人の同意が要る（第3.2条。U-19）

### 確認できたこと（Meshy の規約本文・利用者が提示）

- **第2.9条：Enterprise 以外（Free / Pro / Premium / Ultra）の Customer Input と
  Customer Output は、学習・検証・テスト・改善に使われうると明記されている。**
  外す手段として書かれているのは Order（個別契約）のみ。
  **Tripo（有料なら学習に使わないと明記）と結論が正反対**（`docs/decisions.md` T-5）
- 第3.2条は有料利用者について「非公開に保つ選択ができる」とも書いており、
  第2.9条との関係が本文だけでは確定できない。**照会が必要**（U-20）。
  それまでは安全側（＝有料でも学習利用の対象）として扱う
- **第2.2条：Customer Input に個人を特定できる情報を含めてはならない**（T-6）
- 第3.2条：無料プランは生成物の権利を Meshy が保有し、CC BY 4.0 で利用許諾
- 第3.3条：Community ページに公開した生成物は **CC0** になる（T-7）
- 第2.5条：API経由の生成物は3日で削除（公式ドキュメントの記載を規約で裏づけ）
- 第2.10条・第4条：**月次クレジットは繰越不可**、追加購入分は1年有効、
  料金は USD 建ての**税別**（U-14 解消）
- 第1.1条：**14歳以上**（Tripo は13歳以上）

### 運営の判断（利用者が決定）

- **学習利用の対象から外す必要はない**（`docs/decisions.md` T-9）。
  Meshy 第2.9条をそのまま受け入れる。Enterprise 契約や Order による除外は求めない
  - **U-20・U-22 は A3.5 の関門から外れた。** 2社を対等に比較できる状態になった
  - **帰結：利用同意の説明文言に「作品が事業者のサービス改善・学習に使われうる」ことを
    明記する必要がある**（T-10）。文言は運営が用意する
  - **及ばない範囲**：無料プランでの権利保持（T-1 / T-4）、
    Meshy 第2.2条の個人特定情報の禁止（T-6）、Community の CC0（T-7）、
    年齢要件（T-8）、Tripo の公開範囲の既定（U-17）は引き続き別問題として残る
- プリセットの `unverified_note` を、実際に残っている前提
  （有料契約・配信ホスト・公開範囲）に書き換えた
- **当面は両社とも無料アカウント。有料化は運用開始後**（T-11）。
  A3.5 は無料アカウントで実施する。ここから3つの制約が生じた：
  - **Meshy の無料枠（月100クレジット）では A3.5 の5題材を実施できない**
    （採用プリセットは1件30クレジットで**3件が上限**。月次クレジットは繰越不可）
  - 無料では両社とも生成物の権利を会社が保持する。
    **Tripo は送信した画像の権利まで保持する**ため、試験用画像の選定に条件が付く
  - **U-15（Meshy の無料枠でAPIキーを発行できるか）の保留を解除**（D-6）。
    無料で進める以上、A3.5 の成否を直接決める関門に戻った
- U-23（Tripo の無料枠でクレジット付与・APIキー発行ができるか）、
  U-24（Meshy で追加クレジットを買うと「paid plan」扱いになるか）を追加

### 「学習利用」の文言の突き合わせ

- **両社とも「学習利用」を定義していない。** 動詞は同じ4つ
  （train / validate / test / improve）だが、**目的語と限定句が違う**
- Meshy 第2.9条の目的語は `Services`、限定句なし → **AIモデルの学習に限らない**と読める
- Tripo 第5.2.2条は `as training data` で `any AI Technology` に限定 →
  **不使用の約束は明確だが範囲は狭い**（役務提供のための利用・表示は別途許諾済み）
- Meshy 第2.8条（Aggregated Statistics）は「improve our models」を含むが、
  **Customer Input / Output は含まないと明記**されており第2.9条とは別の話
- 具体的な範囲は条文から読み取れないため U-22 として照会対象にした

### 記録

- `docs/provider-contracts.md` 第0節・第1.6節・第2.2.1節・第2.2.2節・第2.4.1〜2.4.4節・
  第2.6節・第3節
- `docs/decisions.md` A-37 / A-38 / A-39、D-6、T-1〜T-4、U-2 / U-3 / U-4 / U-7 / U-8 / U-9

---

## 0.4.0 — 2026-09-08（A4：品質比較画面・判定表・限定公開）

仕様 Ver.1.1 第16章のA4を実装した。実APIは呼んでいない。

### 追加

- **品質比較画面**（`/comparisons/{id}`）
  - PCは並列、スマホは縦並びで1つずつ切り替え（重いGLBを同時に読み込まない）
  - 正面・横・背面・真上の視点操作、背景色切替、表示リセットをA・Bへ同時に適用
  - 「いまの向きを『正面』にする」で、講師が基準角度をこの端末に覚えさせられる
- **ブラインド評価**（既定ON）
  - 評価が確定するまで、画面HTML・APIレスポンス・配信URL・ファイル名に
    サービス名・モデルID・プリセット名・外部タスクIDを含めない
  - 比較に含まれる全ての結果の評価が確定すると自動で開示する
  - 運営による手動開示（理由必須、監査ログ必須）
  - ブラインドで行った評価かどうかを記録し、集計で区別する
- **判定表**（サービス×題材タグ）
  - サンプル不足／採用候補／保留／不適を、設定値の閾値で自動表示（最終判断は運営）
  - 初回合格率と**品質のみ初回合格率**（技術失敗を分母から除く）を併記
  - 2回以内合格率、重大不具合の発生率、技術完了・未評価・脱落の内訳
  - 採用候補となった題材タグ数と、サービス選定の優先順位の説明
- **集計画面**：受付〜評価待ちの p50/p95、停止件数、保存成功率、講師作業時間、
  ブラインド／非ブラインドの内訳
- **費用画面**：見積累計・手動確認済み実績・失敗要照合・上限額との差。
  確定実費が見積合計を超えたら警告し、新規生成を停止する
- **実費の手動入力**（生成結果画面）。同じ明細を二重に計上しない
- **校正セット**：対象件数と集計に使う評価者を設定でき、2名そろうまで集計に含めない。
  2段階以上の差がある軸を警告する。平均は使わず、指定した1名分だけを使う
- **一覧のポーリング**（15〜30秒）と、題材ごとの状態・判定の表示
- 画面からの2社比較の受付（`POST /comparisons`）
- `deploy/`：Caddyfile 例、限定公開用の compose 上書き、ファイアウォール手順（すべてダミー値）
- `docs/quality-test-plan.md`：実機確認の手順とチェックリスト、自動試験の対応表
- 自動試験を103件に拡充。仕様第13章の必須試験 **1〜20 をすべて実施**
  （12・18 を追加、14・18 は Playwright で画面から確認）

### 変更

- 生成結果画面もブラインドの対象になり、確定までサービス名等を表示しない
- 検証セット画面に集計へのリンクと、比較の一覧を追加

### この版に含まれないもの

A3.5（早期実感触）と A5（実API試験）。いずれも価格とデータ取扱い条件の確認、
配信ホスト・上限額・APIキーの設定が済んでから実施する。

## 0.3.0 — 2026-09-08（A3：Tripo/Meshyアダプター）

仕様 Ver.1.1 第16章のA3を実装した。**実APIは呼んでいない**（`LIVE_API_ENABLED=false`）。
価格とデータ取扱い条件が未確認のため、両社のプリセットは無効のままで実生成に選べない。

### 追加

- `app/providers/tripo.py`：Tripo公式SDK `tripo3d` 0.4.2（MIT）を版固定で使用
  - import 時の位置検出（第三者のIP位置情報サービスへの問い合わせ。うち2つは平文HTTP）を
    `TRIPO_DISABLE_GEO_DETECTION=1` で必ず止める
  - SDKのダウンロード経路は使わない（SSL検証に失敗すると検証を無効にして取得し直す実装のため）
  - SDKは要求ごとのタイムアウトを持たないため `asyncio.wait_for` で仕様第8章の秒数を適用
  - 作成系の自動リトライが無いことをソースで確認（無効化は不要）
  - APIキーの接頭辞 `tsk_` を送信前に確かめ、値そのものは出さない
- `app/providers/meshy.py`：公式CLI `meshy-cli` 0.2.0（MIT）のソースで確認した契約に基づく
  HTTPクライアント実装。画像は `data:` URI で送り、公開URLを作らない
- `app/services/download_guard.py`：成果物取得の保護（仕様第12章）
  - HTTPSのみ、許可ホストのみ（既定は空で、設定しない限り取得しない）
  - 名前解決した全アドレスを検査し、localhost / private / link-local / メタデータ用アドレスを拒否
  - リダイレクトは自動追従せず、1ホップごとに検査し直す
  - ストリーム保存し、サイズ上限を超えたら打ち切る
- 固定プリセット `tripo-standard` / `meshy-standard` を、確認済みのモデルID・パラメーターで登録
  （価格未確認のため無効のまま）。既存の空のプリセット行も `seed-presets` で更新できる
- 設定 `APP_TRIPO_DOWNLOAD_HOSTS` / `APP_MESHY_DOWNLOAD_HOSTS`（既定は空）
- `docs/provider-contracts.md`：確認できたこと・できなかったことの記録（確認日つき）
- 自動試験を86件に拡充（A2の55件＋31件）。ダウンロード保護のSSRF系10件、アダプター21件を新設。
  公式SDKをローカルの偽サーバーに向けて実際に動かす結合試験を含む

### 未確認のまま残していること

1件あたりの価格（クレジット数とUSD単価）、データ保持期間、学習利用の可否とopt-out手段、
生成物の利用条件、成果物の配信ホスト、レート制限の具体値、
Meshy の `DELETE` が実行中タスクの取消になるか。
これらが確認できるまで実生成は選べない（`docs/provider-contracts.md` の第3節）。

## 0.2.0 — 2026-09-08（A2：永続ワーカー・復旧・冪等・上限額）

仕様 Ver.1.1 第16章のA2を実装した。受付と外部送信を分け、web と worker が別プロセスで動く。
生成中にワーカーを再起動しても、同じ外部タスクを再確認して完了する。

### 追加

- 永続ワーカー（`app/worker.py` と `app/services/worker_steps.py`）
  - 1周で「lease回収 → 送信 → 状態確認（全件）→ ダウンロード（1件）」を行う
  - claim は BEGIN IMMEDIATE ＋条件付きUPDATE。`lease_owner` / `lease_until` で競合を防ぐ
  - 外部通信のあいだDBのトランザクションを持たない（claim・通信・書込を別の短期処理に分ける）
  - lease が切れた `submitting` は `submission_unknown` へ移し、自動で再送信しない
  - SIGTERM / SIGINT で処理の切れ目に停止する
- 仕様第8章の状態遷移表のすべての分岐と復帰経路
  - `submission_unknown` / `provider_failed` / `download_failed` / `validation_failed` /
    `monitoring_paused` / `cancelled`
  - 指数バックオフ＋ジッター、429 の Retry-After、30分で監視の一時停止（外部失敗とは断定しない）
- 復帰操作のAPI：`retry`（理由必須・新ID）、`refresh`、`retry-download`、`cancel`、
  `resolve-submission`（ひも付け／未作成確認）、`resolve-artifact`
- 冪等キー（サーバー発行のフォームトークン方式）。同キー・同本文は既存結果、同キー・別本文は409
- 上限額の判定（セット単位・全体）。送信済み全件の上限見積の合計で判定し、
  比較は2社分を合算する。上限額判定・送信枠確認・依頼登録を同一の短期トランザクションで行う
- 送信枠（検証セット内の asset×provider で2回）と、理由付き・監査ログ必須の枠追加
  （`quota_grants` テーブル）。`provider_failed` と未送信取消は返却、`submission_unknown` は保持
- 2社比較の受付（`POST /api/comparisons`）。A/B割当を比較ごとにランダムに固定して保存する
  （画面での秘匿と開示は A4）
- 手動確認した実費の記録（`POST /api/generations/{id}/costs`）。二重計上を防ぐ一意キー付き
  （画面は A4）
- モックの障害切替：成功／遅延／事業者失敗／429／作成応答タイムアウト／長時間待機／
  ダウンロード失敗／不正GLB。上限額超過は見積額を持つプリセットで表す
- 2社比較を検証するためのモック `mock_a` / `mock_b`
- 外部送信の直前にも利用同意を確認する（受付後に同意が取り下げられた場合に備える）
- 生成結果画面の5秒ポーリングと復帰操作のボタン。
  「通信確認待ち」「受付結果不明」「保存失敗」を生成失敗と区別して表示し、
  追加の課金が発生するのは再生成だけであることを明記する
- 自動試験を55件に拡充。仕様第13章の必須試験 1〜11、13、15、16、17、19、20 を実施

### 変更

- 生成の受付は `queued` を作るだけになり、外部送信はワーカーが行う（A1は同期処理だった）
- 日時列を `UtcDateTime` にし、SQLite から読み出した値にもUTCが付くようにした
- `begin_immediate()` は同じセッションで複数回呼ばれても最初の1回だけ効く

### この版に含まれないもの

ブラインド評価の画面と開示、判定表、校正セット、費用画面と手動実績入力の画面、
一覧のポーリング、Tripo/Meshyアダプター、限定公開環境。いずれも A3・A4 で実装する。

## 0.1.0 — 2026-09-08（A1：骨組み・モック）

仕様 Ver.1.1 の第16章 A1 を実装した。モックでログイン→画像→疑似生成→GLB→評価→CSVまで通る。

### 追加

- プロジェクト基盤：`pyproject.toml`、`uv.lock`、`Dockerfile`、`compose.yaml`、`.env.example`、`.gitignore`
- 仕様第9章のデータ設計に基づく全13テーブルと、Alembic の初期マイグレーション
  （フェーズAで未使用の列も含めて作成）
- ログイン（argon2id、HttpOnly Cookieセッション、有効期限、ログアウト、CSRF二重送信検証）
- 運用CLI：`init-db`、`create-operator`、`seed-presets`。初期パスワードの固定値は持たない
- 検証セットの作成と集計表示
- 画像登録：デコード検査、PNG/JPEG/WebPのみ、10MiB・20メガピクセル上限、
  EXIF方向の正規化と位置情報を除去した送信用コピーの生成
- 利用同意（同意あり／不要／未取得）の記録と更新。更新は理由必須で監査ログに残る
- 加工版の追加。加工種別は選択式（複数可）、ツール名、メモ
- Mockアダプター（成功系）と、受付から評価待ちまでの最短経路
- GLBの検査：形式ヘッダー、宣言長との整合、チャンク構造、外部URI参照の拒否。
  併せて形状メトリクス（頂点数・三角形数・メッシュ数・マテリアル数・
  テクスチャ枚数と解像度・バウンディングボックス）をローカル抽出
- 3D表示：model-viewer 4.3.1 を固定してローカル配信。視点操作、背景色切替、表示リセット。
  読込失敗と長時間待ちを表示し、操作不能・永久ローディングにしない
- 5段階評価（5軸）、重大不具合タグ、判定、作業秒。評価改訂は履歴として追加する
- CSV出力：UTF-8 BOM、数式エスケープ、保存キーやURLを含めない
- 認証つきファイル配信（画像・送信用コピー・GLB）。パストラバーサル禁止
- ワーカーの入口（`app/worker.py`）。A1では取り残された受付を拾う保険として動く
- 自動試験25件。うち仕様第13章の必須試験10・11を含む

### この版に含まれないもの

永続ワーカー、再起動復旧、lease、冪等キー、上限額判定、送信枠、2社比較、
ブラインド評価、判定表、校正セット、Tripo/Meshyアダプター、限定公開環境。
いずれも A2〜A4 で実装する（`docs/plan.md`）。

### 注記

- モックの結果は品質実績ではない。実APIは一度も呼んでいない
- Tripo / Meshy のプリセットは「未確認」として無効のまま登録している
