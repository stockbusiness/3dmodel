# CLAUDE.md — 3Dアート品質検証システム（stockbusiness/3dmodel）

このリポジトリはClaude Codeで実装する。作業前に必ずこのファイルと `docs/spec/3d_art_validation_spec_v1_1.md` を読むこと。

## 1. 正本と優先順位

1. `docs/spec/3d_art_validation_spec_v1_1.md`（仕様書 Ver.1.1）が唯一の正本。
2. このCLAUDE.mdは作業ルール。仕様と矛盾したらこのファイルを修正提案し、仕様を勝手に変えない。
3. `docs/decisions.md` に、仕様に書かれていない判断をすべて記録する（下記の分類タグ付き）。
4. `docs/provider-contracts.md` に、外部API（Tripo/Meshy）の確認結果を記録する。

## 2. 分類タグ（未決事項を推測で確定させない）

仕様に無い・確認できない事項は、実装せずに以下のタグで `docs/decisions.md` に記録する。

- `BLOCKER`：これが決まらないと次に進めない。ユーザーの入力が必要。
- `ASSUMPTION`：軽微な選択を自分で決めた。理由と影響範囲を書く。後から覆せる形で実装する。
- `DEFERRED`：仕様上フェーズB/C、または今回対象外と判断して後送りにした。
- `SECURITY_REVIEW`：セキュリティ上ユーザーの確認が要る。
- `UNVERIFIED`：外部APIの仕様・モデルID・価格・SDK引数を公式資料で確認できていない。画面上は「未確認」表示にし、実生成を有効化しない。

存在しないSDK機能・モデル名・価格を推測して実装することは禁止。確認できなければ `UNVERIFIED` にする。

## 3. 技術構成（仕様第3章、変更禁止）

- Python 3.11 / FastAPI / Uvicorn / Jinja2 / 通常のJS・CSS（Reactは使わない）
- SQLite + SQLAlchemy 2系 + Alembic（外部キー有効、WAL、busy timeout）
- worker は同一コードベースの別プロセス（`app/worker.py`）
- Google model-viewer はバージョン固定でローカル配信。CDNのlatestを動的取得しない
- Docker Compose で web + worker、共通 data ボリューム
- pytest、画面はPlaywright
- ディレクトリ構成は仕様第15章に従う

## 4. Git運用

- `main` への直接pushは禁止。ステージごとにブランチを切る：`feature/a1-skeleton`、`feature/a2-worker`、`feature/a3-providers`、`feature/a3-5-early-check`、`feature/a4-review-ui`、`feature/a5-live-test`
- 1回の作業は1ステージ以下。ステージをまたいで先回りしない（仕様第16章）。
- 作業開始時に `git status` と既存ファイルを確認し、ユーザーの未コミット変更を破壊しない。
- 各ステージ完了時に `VERSION` を上げ、`CHANGELOG.md` に日本語で追記する。
- コミットメッセージは日本語可。1コミット1目的。

## 5. 秘密情報・外部通信

- APIキーは環境変数のみ。`.env` はコミット禁止。`.env.example` はダミー値のみ。
- キー、Bearerヘッダー、画像base64、署名URL、パスワード、事業者レスポンス原文をログ・チャット・テスト出力・コミットに出さない。
- `LIVE_API_ENABLED=false` が既定。テストは実APIを呼ばない。実API試験はユーザーが明示的に有効化し、上限額を指定した場合のみ。
- 利用者入力のURLをサーバーに取得させない。事業者からのダウンロードは許可ホストのみ（仕様第12章）。
- 既存AIアート教室のリポジトリ（team478a/ai-art-school）と本番環境には一切触れない。

## 6. 実装時の必須挙動（仕様から特に落としやすい点）

- 送信前に `submitting` をコミットし、タスクIDを得たら直ちに保存。タイムアウト後に自動で再POSTしない。
- 送信枠の消費・返却は仕様第8章の状態遷移表に厳密に従う（provider_failed は返却、submission_unknown は保持）。
- 冪等キーはサーバー発行のフォームトークン方式。同キー・別本文は409。
- 上限額判定と送信枠確認と依頼登録は同一の短期トランザクション。DBロックを持ったまま外部通信しない。
- ブラインド評価中は画面・API・URL・ファイル名にサービス名・モデルID・プリセット名を出さない。
- 利用同意 `missing` の画像はサーバー側でも外部送信を拒否する。
- モックと実API、未評価と合格、ブラインドと非ブラインド、早期確認と通常検証を集計で混ぜない。
- 金額は整数（micro-USD）。浮動小数点で持たない。
- 画面は日本語。本文16px以上、スマホのボタン44px以上。

## 7. 報告

- 報告・コメント・ドキュメントは日本語。
- 完了報告は仕様第17章の形式に従い、「実装済み」「モック検証済み」「実API検証済み」「未確認」を必ず分ける。
- モック結果を品質実績として書かない。架空の成功率・待ち時間・費用を書かない。
- 計画の提示だけで終わらず、動くコード・テスト・起動手順まで完成させる。
- 途中で `BLOCKER` に当たったら、そのステージで実装できる範囲を完成させてから、`BLOCKER` を列挙して止まる。
